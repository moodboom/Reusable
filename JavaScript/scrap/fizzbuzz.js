#!/usr/bin/env node

// find the longest palindrome
// in an input string

var find_pal = function(in)
{
  int bestlen = 0
  int beststart = -1
  function is_pal(in,startchar)
  {

  }
  for (var startchar = 0; startchar < in.length(); startchar++)
  {
    if (is_pal(in.substring(startchar)))
  }
}

var count = 100;
for (var i=0; i<100; i++)
{
  if (i%5==0) console.log(i+" is a fizz multiple of 5");
  if (i%3==0) console.log(i+" is a buzz multiple of 3");
}
